export type Engine = 'diff-drift' | 'rule-file';
export type GroundTruth = 'positive' | 'negative';

export interface CorpusScenario {
  id: string;
  description: string;
  filePath: string;
  engine: Engine;
  detectorUnderTest: string;
  groundTruth: GroundTruth;
  note?: string;
}

// 18 synthetic PR scenarios: 8 true-positive cases (covering all six detectors,
// with an extra code-path variant each for DD-3 and DD-4), 4 genuinely benign
// changes to monitored files, 5 near-miss cases designed to stress-test false
// positives, and 1 known-gap case designed to stress-test a false negative.
// Composition is deliberately not skewed toward cases known to pass -- see
// RESULTS.md for the honest false-positive/false-negative writeups.
export const SCENARIOS: CorpusScenario[] = [
  {
    id: 'dd1-new-server',
    description: 'A brand-new MCP server entry is added alongside an untouched existing one.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'dd2-command-swap',
    description: "MCPoison pattern: an already-approved server's args are silently repointed.",
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'dd3-wildcard-added',
    description: 'A wildcard permission is introduced into the allow-list (high-severity path).',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
  },
  {
    id: 'dd3-plain-allow-added',
    description: 'A non-wildcard permission is added to the allow-list (warning-severity path).',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
  },
  {
    id: 'dd4-hook-added',
    description: 'A brand-new hook is added under an event that had no hooks before.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
  },
  {
    id: 'dd4-hook-command-changed',
    description: "CVE-2025-59536 pattern: an existing hook's command is replaced with a malicious one.",
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
  },
  {
    id: 'rf1-zero-width-space',
    description: 'A zero-width space is hidden inside an instruction-hijacking sentence in CLAUDE.md.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
  },
  {
    id: 'rf2-cyrillic-homoglyph',
    description: "A Cyrillic 'a' look-alike is substituted into a permission-widening sentence.",
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
  },
  {
    id: 'benign-mcp-reorder',
    description: 'Two existing MCP servers are reordered and an unrelated top-level field is added; no server fields change.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'Stresses whether DD-1/DD-2 are order-sensitive at the object level (they should not be; comparisons are set/key based).',
  },
  {
    id: 'benign-claude-md-doc-addition',
    description: 'A plain-English documentation section is added to CLAUDE.md, no special characters.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
  },
  {
    id: 'benign-permissions-narrowing',
    description: 'An allow entry is removed and a deny rule is added for the same action -- a pure narrowing.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'negative',
  },
  {
    id: 'benign-copilot-instructions-edit',
    description: 'A harmless style-preference sentence is added to copilot-instructions.md.',
    filePath: '.github/copilot-instructions.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
  },
  {
    id: 'near-miss-args-reorder',
    description: "An MCP server's two independent CLI flags are reordered; the positional package argument stays last and behavior is unchanged.",
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'negative',
    note: 'DD-2 compares args via JSON.stringify, which is order-sensitive by design (see the code comment: reordering CAN change execution semantics for positional CLI args, so it is deliberately treated as drift). Included to test that documented tradeoff honestly rather than assume it away.',
  },
  {
    id: 'near-miss-hook-removed',
    description: 'An existing hook is deleted outright, with no other hooks changed.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'DD-4 is scoped to "added or changed" per architecture.md 5; a pure removal is out of its stated scope, so silence here is correct, not a miss.',
  },
  {
    id: 'near-miss-bom',
    description: 'A UTF-8 byte-order-mark (U+FEFF) is prepended to CLAUDE.md, a common artifact of certain editors saving the file, with no other changes.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
    note: 'Stresses whether RF-1 over-fires on invisible-adjacent characters outside its documented U+200B-200D/202A-202E/2066-2069 ranges.',
  },
  {
    id: 'near-miss-legit-cyrillic-text',
    description: 'A genuine Russian-language example sentence is added to CLAUDE.md as localization documentation -- not an attack.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'negative',
    note: 'RF-2 is a pure character-class check with no natural-language awareness (architecture.md 5), so it cannot distinguish a homoglyph substituted into Latin text from a legitimate sentence written entirely in Cyrillic.',
  },
  {
    id: 'near-miss-mcp-server-rename',
    description: 'An existing MCP server is renamed to a clearer key; command and args are byte-for-byte identical.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'DD-1 diffs by key name only, so it cannot tell a rename of a trusted entry apart from a genuinely new, unreviewed one.',
  },
  {
    id: 'known-gap-uncommon-homoglyph',
    description: "A homoglyph attack using Cyrillic U+0501 ('d' look-alike), a code point not in RF-2's confusable table.",
    filePath: '.cursor/rules/deploy.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "This is a real attack pattern. RF-2's table covers well-documented confusables, not the full Unicode confusables database, per architecture.md 2's accepted precision-over-recall tradeoff: RedFlag CI will miss cleverly obfuscated attacks outside its deterministic checks.",
  },

  // --- Session 1 of 6 (corpus expansion 18 -> 113): 21 additional scenarios,
  // 5 DD-1, 4 DD-2, 6 DD-3, 6 DD-4. See TASK_REPORT for this session's full
  // verification against real detector output, including documented
  // discrepancies between stated ground truth and actual behavior that were
  // deliberately NOT force-fixed.

  {
    id: 'dd1-servers-key-variant',
    description: 'A new MCP server is added under the "servers" key instead of "mcpServers".',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Confirms DD-1 reads either key name (obj.mcpServers ?? obj.servers), not just the more common one.',
  },
  {
    id: 'dd1-minimal-config',
    description: 'A new MCP server entry has only a "command" field, no "args".',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'DD-1 keys off server names only, so a minimal entry shape should not matter.',
  },
  {
    id: 'dd1-multiple-servers-added',
    description: 'Two distinct new MCP servers are added in a single diff.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Expect exactly 2 findings, one per new server.',
  },
  {
    id: 'dd1-unicode-server-name',
    description: 'A new MCP server is keyed with a name containing a space and an accented Unicode character.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'DD-1 does a plain Set/key comparison, so it should not care about the character class of the server name.',
  },
  {
    id: 'dd1-server-added-trailing-whitespace-key',
    description: 'A new server key is a trailing-whitespace variant of an existing, visually near-identical key ("filesystem " vs "filesystem").',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Set membership is exact-string, so a trailing space makes this a distinct, new key -- correctly flagged, not silently treated as the same server.',
  },
  {
    id: 'dd2-args-only-change',
    description: 'An existing server\'s command is unchanged; only its args array changes.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'dd2-both-changed',
    description: 'An existing server has both its command and args replaced.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'dd2-env-var-change',
    description: 'An existing server\'s "env" field changes (widening a filesystem server\'s allowed directory from /workspace to /); command and args are untouched.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "Documented, accepted scope gap, not a bug to fix here: DD-2's PINNED_FIELDS is exactly ['command', 'args', 'version', 'hash'] (architecture.md 5 names only command/args/version). 'env' is never compared, so a security-relevant env-var change on an already-approved server goes undetected. This is an expected false negative, analogous to known-gap-uncommon-homoglyph.",
  },
  {
    id: 'dd2-version-hash-change',
    description: 'An existing server\'s pinned "version" field changes; command and args are untouched.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'dd3-deny-removed-plain',
    description: 'A single non-wildcard deny rule is removed; nothing else in the file changes.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
  },
  {
    id: 'dd3-multiple-allow-added',
    description: 'Two new allow-list entries are added in a single diff.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'Expect exactly 2 findings, one per added allow entry.',
  },
  {
    id: 'dd3-permissions-block-newly-created',
    description: 'The base file has no "permissions" key at all; head adds one with a single allow entry.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'An absent permissions block parses as empty allow/deny (fail-open, not a parse failure), so every entry in a newly-created block reads as newly added.',
  },
  {
    id: 'dd3-allow-and-deny-both-changed',
    description: 'One allow entry is added and one deny rule is removed in the same diff.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'Expect exactly 2 findings: one allow-added, one deny-removed.',
  },
  {
    id: 'dd3-wildcard-narrowed-to-specific',
    description: 'An existing "Bash(*)" allow entry is replaced with the narrower "Bash(npm test)".',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'negative',
    note: 'Intended as a narrowing (a wildcard replaced by one specific command) that should not fire. DD-3 only checks whether each head allow string is present in base\'s allow set -- it does not correlate a removal with an addition, so "Bash(npm test)" reads as a brand-new allow entry regardless of what it replaced. See task report for whether this reproduced.',
  },
  {
    id: 'dd3-narrowing-syntax-rewrite',
    description: 'An existing "Write(*)" allow entry is reworded to "Write(./dist/**)", scoping writes to one directory.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'negative',
    note: 'Intended as a narrowing. The replacement string still contains a literal "*" character (from the "**" glob), so if it fires at all, WILDCARD_CHAR\'s substring check would escalate it to high severity. See task report for whether this reproduced.',
  },
  {
    id: 'dd4-multiple-hooks-changed',
    description: 'Two separate hooks in the same array-valued event both get their command changed in one diff.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: 'Direct regression test for the array-indexing fix in Task 3.4 (commit 611103c, "stable index keying + clean display names"): expect exactly 2 findings, PreToolUse[0] and PreToolUse[1], each reporting its own correct old-to-new command, not cross-contaminated between positions.',
  },
  {
    id: 'dd4-hook-removed-single',
    description: 'An existing plain string-valued hook is deleted outright; a sibling hook is untouched.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'Same "removed hooks are out of DD-4\'s added/changed scope" reasoning as the existing near-miss-hook-removed scenario, but exercised via the plain-string hook shape instead of the array-of-objects shape, to check the two code paths agree. See task report for the consistency check.',
  },
  {
    id: 'dd4-matcher-changed-command-same',
    description: 'A hook\'s "matcher" is broadened from "Bash" to "*"; its command is byte-for-byte unchanged.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "Label corrected: this was originally 'negative' because DD-4's spec (architecture.md 5) named only 'a change to an existing hook's command' -- matcher was never read for comparison, so under that old spec this genuinely shouldn't fire. The spec has since been updated to explicitly cover 'a change to [a hook's] matcher/trigger scope' as well, since broadening a hook's matcher (e.g. 'Bash' to '*') is a real widening of the hook's effective reach independent of its command. hookChanged.ts was fixed to match. The stale 'negative' label was a leftover from the pre-fix spec, not a reflection of correct detector behavior; the detector's current firing on this scenario is correct, and the label now says so.",
  },
  {
    id: 'dd4-command-whitespace-only-change',
    description: 'A hook\'s command changes only in whitespace ("npm test" to "npm  test", an extra space), with no semantic change to what runs.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'JUDGMENT CALL, documented rather than fixed: labeled negative on the theory that a purely cosmetic whitespace diff should not warrant a high-severity CVE-2025-59536-pattern alert (noise risk), but the command comparison is strict string inequality with no normalization, so this is expected to fire in practice. See task report for whether this reproduced.',
  },
  {
    id: 'dd4-array-vs-object-shape',
    description: 'A hook defined as a bare object in base ({"matcher":..., "command":...}) is rewritten as a one-element array with the identical command in head.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "Object-shape entries key as the bare eventName (e.g. 'PreToolUse'); array-shape entries always key as 'eventName[i]' (e.g. 'PreToolUse[0]'), even for a single element. The two shapes for the same event therefore never match by key, so this is expected to fire -- but as a 'New hook added' finding, not a 'command changed' one, even though the command never changed. See task report.",
  },
  {
    id: 'dd4-new-hook-event-type',
    description: 'A hook is added under an event type ("SessionStart") that had no hooks section at all in base.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
  },

  // --- Session 2 of 6 (corpus expansion 39 -> 113): 19 additional scenarios,
  // 4 RF-1, 5 RF-2, and 10 JUDGMENT-labeled cases documenting actual behavior
  // on genuinely debatable or underspecified inputs rather than asserting a
  // "correct" answer. None of these 10 were judged to be a genuine defect
  // (as opposed to a documented tradeoff of an already-accepted design
  // choice); see TASK_REPORT for the full reasoning, especially the
  // dual-schema-key precedence case, which was the most surprising result.

  {
    id: 'rf1-zero-width-joiner',
    description: 'A zero-width joiner (U+200D), isolated from any other invisible character, is hidden inside an instruction-hijacking sentence.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
  },
  {
    id: 'rf1-zero-width-non-joiner',
    description: 'A zero-width non-joiner (U+200C), isolated from any other invisible character, is hidden inside an instruction-hijacking sentence.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
  },
  {
    id: 'rf1-bidi-rlo-specific',
    description: 'A right-to-left override (U+202E), isolated from any other invisible character, is inserted mid-sentence.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
  },
  {
    id: 'rf1-multiple-bidi-variants-one-file',
    description: 'Three distinct bidi-control characters (U+202A, U+202D, U+2066) appear together in one file.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: 'Expect exactly 3 findings, one per distinct character occurrence, since the detector regex is global and matches every occurrence independently.',
  },
  {
    id: 'rf2-greek-homoglyph',
    description: "A Greek omicron (U+03BF) is substituted for a Latin 'o' in otherwise-English text.",
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "Greek lowercase alpha (U+03B1) is NOT in RF-2's confusable table (only lowercase omicron, U+03BF, is present for Greek lowercase); omicron was used specifically so this scenario is a real TP rather than an accidental known-gap.",
  },
  {
    id: 'rf2-multiple-scripts-mixed',
    description: 'A Cyrillic homoglyph and a Greek homoglyph both appear in the same file.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: 'Expect exactly 2 findings, one labeled Cyrillic and one labeled Greek.',
  },
  {
    id: 'rf2-homoglyph-in-code-fence',
    description: 'A Cyrillic homoglyph appears inside a triple-backtick markdown code block in CLAUDE.md.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: 'RF-2 has no markdown parsing or code-fence awareness (a raw character-class scan over the whole file per architecture.md 5), so content inside a code block is not treated any differently from prose.',
  },
  {
    id: 'rf2-homoglyph-adjacent-attack-text',
    description: 'A homoglyph is used to construct a full, plausible hidden-instruction sentence ("ignore all previous safety instructions..."), not just an isolated character dropped into unrelated prose.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
  },
  {
    id: 'rf2-homoglyph-in-url',
    description: 'A Cyrillic homoglyph is substituted into a domain name inside a URL embedded in CLAUDE.md (an IDN-style domain-spoofing pattern).',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
  },
  {
    id: 'judgment-dd3-deny-removed-had-wildcard',
    description: 'A removed deny rule itself contained a wildcard ("Bash(*)" removed from deny).',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'Post-fix regression check for the wildcard-escalation fix: confirms deny-removal stays at warning unconditionally regardless of wildcard content, since that fix only touched the isUnrestrictedWildcard condition inside the allow-added loop, not the separate deny-removed loop. Mirrors an existing unit test of the same name/intent, exercised here as a corpus fixture instead.',
  },
  {
    id: 'judgment-dd1-server-re-added-identically',
    description: 'Simulates a server that was present, removed, then re-added identically, by having base lack it and head have it (a two-state diff cannot literally encode three historical states).',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: "JUDGMENT: this is structurally indistinguishable from a plain new-server addition, and that is the point. v1 is stateless by design (architecture.md 2: no persisted baseline beyond the PR's own base branch), so DD-1 has no way to know a server was previously approved and silently reinstated versus genuinely new. Documented as an inherent consequence of the stated stateless design, not a bug.",
  },
  {
    id: 'judgment-dd2-args-reorder-with-real-semantics',
    description: "An MCP server's args are reordered such that a positional flag value moves before its flag ([\"--config\", \"/etc/app.conf\", ...] becomes [\"/etc/app.conf\", \"--config\", ...]), which genuinely breaks argument parsing, unlike near-miss-args-reorder's two independent, order-irrelevant flags.",
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "JUDGMENT (contrast case): DD-2's order-sensitive comparison fires identically here and on near-miss-args-reorder -- the detector cannot distinguish a semantically meaningful reorder from a cosmetic one. This scenario is the case where that same mechanism is unambiguously correct to fire.",
  },
  {
    id: 'judgment-dd4-hook-whitespace-only',
    description: 'A hook command changes only in whitespace (an extra space before a flag), a second, independent example of the same class as the existing dd4-command-whitespace-only-change scenario.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'JUDGMENT: re-confirms Session 1\'s dd4-command-whitespace-only-change finding with independent fixture content -- strict string inequality with no whitespace normalization means this is expected to fire in practice despite being labeled negative on noise-avoidance grounds.',
  },
  {
    id: 'judgment-dd1-both-schema-keys-present',
    description: 'A file has both "mcpServers" and "servers" top-level keys simultaneously; a new server is added under "servers" while "mcpServers" (present but empty) is unchanged.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: "JUDGMENT, and the most surprising Session 2 result: parseMcpServers uses obj.mcpServers ?? obj.servers. Nullish coalescing means an EMPTY mcpServers object ({}) still short-circuits the fallback -- \"servers\" is never even read once \"mcpServers\" exists at all, regardless of its content. A new entry added under the non-precedent key is completely invisible to DD-1. This is a contrived, low-realism construction (no mainstream tool writes both keys with divergent content), so it is documented as a judgment call rather than escalated as a defect, but it is worth knowing about.",
  },
  {
    id: 'judgment-rf1-invisible-char-in-security-docs',
    description: "A zero-width space appears inside CLAUDE.md within a new Security Note section that is itself explaining and demonstrating this exact attack vector.",
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: 'JUDGMENT (confirmatory): fires anyway, as expected. RF-1 is a pure character-class check with no natural-language awareness (architecture.md 5), so it cannot tell a genuine attack from a demonstration example inside documentation explaining the attack. Flagging every occurrence regardless of surrounding context is the correct, conservative default here, not a false positive worth softening -- demonstration invisible characters in real docs are rare enough that always flagging is the safer choice.',
  },
  {
    id: 'judgment-dd3-wildcard-added-then-narrowed-same-pr',
    description: 'Within one diff: a new wildcard allow entry ("Write(*)") is added, and a separate, unrelated allow entry is narrowed ("Bash(*)" replaced by "Bash(npm test)"), in the same file.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'JUDGMENT (confirmatory): expect exactly 2 findings, evaluated independently per entry -- one warning for the plain "Bash(npm test)" addition, one high for the "Write(*)" wildcard addition. The narrowing side (Bash(*) disappearing) is not tracked, consistent with dd3-wildcard-narrowed-to-specific from Session 1.',
  },
  {
    id: 'judgment-rf2-latin-loanword-in-cyrillic-context',
    description: 'A genuine Russian sentence ("Команда для сохранения:") correctly embeds an English technical term ("git commit") in Latin script, as is standard practice for CLI command names in non-English technical writing.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'negative',
    note: "JUDGMENT, symmetric to near-miss-legit-cyrillic-text: RF-2's character class only matches Cyrillic/Greek code points, never Latin ones, so the embedded \"git commit\" loanword itself cannot trigger it either way. The surrounding genuine Cyrillic prose does, for the exact same structural reason as the existing near-miss case -- whether or not a Latin loanword is present is irrelevant to the outcome.",
  },
  {
    id: 'judgment-dd4-hook-command-becomes-empty-string',
    description: "An existing hook's command changes to an empty string, disabling it without removing the hook's key.",
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: 'JUDGMENT: fires as "command changed" (non-empty !== empty), the same as any other command edit, even though the practical effect here is risk-reducing (disabling the hook) rather than risk-increasing. DD-4 does not and cannot reason about the direction of a change, only that the string differs -- arguably the conservative, correct default (flag every hook edit for human confirmation) rather than a flaw.',
  },
  {
    id: 'judgment-dd3-permission-entry-case-difference',
    description: 'An allow entry\'s casing changes only ("Bash(npm test)" becomes "Bash(NPM TEST)"), with no other changes.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'JUDGMENT: comparison is case-sensitive (plain Set/string equality, no normalization anywhere in the parser), so the differently-cased string reads as a brand-new allow entry (warning; its body is not all-asterisk so it does not escalate) while the old-cased entry\'s disappearance goes untracked, same "exact-string comparison" root cause already accepted for dd1-server-added-trailing-whitespace-key in Session 1.',
  },

  // --- Session 3 of 6 (corpus expansion 58 -> 113): 20 additional scenarios,
  // 14 exercising architecture.md section 2's fail-open guarantee (all
  // expected: zero findings, no thrown error, across all four diff-drift
  // detector functions -- not just the nominal detectorUnderTest, since a
  // malformed/wrong-shaped file is fed to all four in production) plus one
  // large-file case confirming fail-open doesn't mean fail-slow, and 6 scale
  // edge cases confirming detection still works correctly at realistic-to-
  // extreme scale. See TASK_REPORT for the full verification, including how
  // the null-byte and invalid-UTF-8 fixtures were constructed safely.

  {
    id: 'failopen-truncated-json-head',
    description: 'Head content is valid-looking JSON cut off mid-object.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'Fail-open (architecture.md 2): JSON.parse throws inside a try/catch in every diff-drift parser, returning null; all four detector functions were called directly and confirmed to return [] without throwing.',
  },
  {
    id: 'failopen-truncated-json-base',
    description: 'Base content is valid-looking JSON cut off mid-object; head is well-formed and adds a real server.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: "Fail-open: a malformed base suppresses output even when head has a genuine new entry, since every detector's base-side parse failure returns [] before comparing anything. This is the documented tradeoff of fail-open over fail-loud -- confirmed, not a bug.",
  },
  {
    id: 'failopen-wrong-type-array-head',
    description: 'Head content is a syntactically valid JSON array, not an object.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'Every parser explicitly checks Array.isArray(parsed) on the top level and treats it as malformed (returns null), since arrays are typeof "object" in JS and would otherwise slip past a naive object check.',
  },
  {
    id: 'failopen-wrong-type-string-head',
    description: 'Head content is a syntactically valid bare JSON string literal.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
  },
  {
    id: 'failopen-wrong-type-number-head',
    description: 'Head content is a syntactically valid bare JSON number.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
  },
  {
    id: 'failopen-wrong-type-null-head',
    description: 'Head content is the literal JSON value null.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'typeof null === "object" in JS, a well-known quirk; every parser explicitly checks parsed === null as a separate condition rather than relying on the typeof check alone, so this is correctly treated as malformed rather than accidentally read as a valid empty object.',
  },
  {
    id: 'failopen-empty-file-head',
    description: 'Head content is a genuine zero-length file.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'DD-1 short-circuits on the "!headContent" falsy check before ever calling JSON.parse (an empty string is falsy in JS); DD-2/DD-3/DD-4 instead reach JSON.parse("") which throws and is caught, also returning null. Two different code paths, same fail-open outcome -- confirmed for all four.',
  },
  {
    id: 'failopen-empty-file-base',
    description: 'Base content is a genuine zero-length file; head is well-formed with real content.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'An empty base string is not the same as a null base (which means "file did not exist" and defaults to an empty server set, so head\'s entries would read as new). An empty STRING fails JSON.parse and is treated as malformed, suppressing all four detectors\' output entirely -- correct fail-open behavior for a genuinely unparseable file, not a bug.',
  },
  {
    id: 'failopen-trailing-comma-json',
    description: 'Head is otherwise-valid JSON with trailing commas after each object/array element, which strict JSON.parse rejects.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
  },
  {
    id: 'failopen-deeply-nested-unexpected-structure',
    description: '"mcpServers" is a syntactically valid array-of-arrays where an object was expected, unchanged between base and head.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'This is NOT a parse failure (the JSON is well-formed): the parsers\' explicit Array.isArray(serverObj) guard treats a wrong-shaped mcpServers value as gracefully-degraded-to-empty (an empty Set/object, not null), which is truthy, so detectNewMcpServer does not even hit its early-return path -- it loops over zero entries instead. Confirms field-level graceful degradation, a level below whole-file fail-open, working as documented in the code comments.',
  },
  {
    id: 'failopen-null-byte-embedded',
    description: 'Valid JSON containing a \u0000 escape sequence inside a string value (a literal NUL character once parsed); base and head are identical.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'negative',
    note: 'Constructed via the JSON \u0000 escape (six plain ASCII characters on disk), which JSON.parse materializes into a real NUL character at runtime -- verified directly (parsed command string has length 4 with char codes 110,112,120,0). Base and head are byte-identical, so this confirms a NUL character survives JSON.parse and string/Set equality checks without throwing or producing a spurious diff, not that detection fires on it.',
  },
  {
    id: 'failopen-non-utf8-bytes',
    description: 'Head contains a raw invalid UTF-8 byte (0xFF, never valid standalone or as a lead byte) spliced into a "_comment" field that no detector reads; base is the same file without the invalid byte.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: "Constructed via a raw Buffer write (fs.writeFileSync with a Buffer, not a text encoding), since a text-based heredoc cannot reliably hold an invalid byte. Reading it back with fs.readFileSync(path, 'utf-8') -- the same decode step fileVersions.ts uses in production -- does not throw; it silently substitutes U+FFFD, confirmed directly, after which JSON.parse succeeds normally. Detector functions never see raw bytes, only already-decoded strings, so this really tests that the upstream decode-then-parse pipeline degrades gracefully, not the detectors in isolation.",
  },
  {
    id: 'failopen-large-file-500kb',
    description: 'A syntactically valid ~490KB .mcp.json (2500 filler servers) with one additional real new-server entry in head.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Not a fail-open case -- the JSON is entirely well-formed. Tests that fail-open does not mean fail-slow: DD-1 correctly finds the single real "monitoring-agent" addition among 2500 unchanged filler entries, and none of the four detectors throw or time out at this scale.',
  },
  {
    id: 'failopen-both-sides-malformed',
    description: 'Base and head are both malformed JSON simultaneously (different malformations on each side).',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'DD-1 short-circuits on head\'s parse failure before ever looking at base; DD-2/DD-3/DD-4 attempt both sides and get null from each, short-circuiting on whichever is checked first. All four confirmed to return [] without throwing regardless of which side is checked first.',
  },
  {
    id: 'scale-large-allow-list-20-entries',
    description: 'A wildcard ("Bash(*)") is added to .claude/settings.json among 20 pre-existing, unrelated allow entries.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: 'Expect exactly 1 finding (the wildcard addition, high severity); all 20 pre-existing entries are correctly recognized as already present in base and produce no noise.',
  },
  {
    id: 'scale-deeply-nested-hook-config',
    description: 'A hooks block with three event types, multiple array entries, and extra per-hook fields (description, timeout, enabled) beyond the canonical matcher/command shape; one hook\'s command is changed to a malicious one.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: 'Confirms the extra, non-canonical fields are silently and correctly ignored (the parser only ever reads matcher/command/name/event), and the one real command change among several sibling hooks is still found precisely.',
  },
  {
    id: 'scale-long-file-buried-invisible-char',
    description: 'A ~14.6KB CLAUDE.md (233 lines) with a single zero-width space buried on line 212.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: 'Confirms the regex scan finds a single invisible character deep into a large file; RF-1\'s line/column locator was also confirmed to correctly report line 212, not an off-by-some-large-amount error.',
  },
  {
    id: 'scale-many-mcp-servers',
    description: '12 pre-existing MCP servers in .mcp.json; one new server ("monitoring-agent") is added.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Expect exactly 1 finding; all 12 pre-existing, unchanged servers produce no noise from either DD-1 or DD-2.',
  },
  {
    id: 'scale-repeated-identical-hooks',
    description: 'Base has two byte-for-byte identical hook entries in the same PreToolUse array; head removes one, leaving one identical survivor.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'Documents actual behavior rather than asserting it: the eventCounts map takes the max array length across base AND head (2, from base), so the surviving head entry is displayed as "PreToolUse[0]" and correctly matched against base\'s PreToolUse[0] (identical command, no finding); the removed PreToolUse[1] is simply never iterated since the outer loop only walks head\'s list, consistent with removals being out of DD-4\'s scope elsewhere in this corpus. Duplicate-content siblings do not confuse the positional key matching.',
  },
  {
    id: 'scale-minified-single-line-json',
    description: 'A fully minified, single-line, whitespace-free .mcp.json gets one new server added, also minified.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'JSON.parse is whitespace-agnostic, so formatting has no bearing on detection; included to confirm nothing in the pipeline assumes pretty-printed input (e.g. no accidental reliance on newlines for parsing).',
  },

  // --- Session 4 of 6 (corpus expansion 78 -> 113): 21 additional scenarios
  // (the task specified 20; #20, "all four diff-drift detectors in one PR
  // across two files," was split into two scenario IDs -- one .mcp.json, one
  // .claude/settings.json -- since this corpus's format is one file-pair per
  // scenario ID, and forcing two files into one fixture directory would
  // break that convention rather than honor it; see the session's chat report
  // for the full reasoning). 14 exercise
  // RF-1/RF-2 against unusual encodings, several DELIBERATELY documenting
  // known-gap false negatives (labeled FN-expected, same convention as the
  // existing known-gap-uncommon-homoglyph), not bugs to fix here. 6 (7 with
  // the split) exercise multiple detectors colliding on one file/PR.

  {
    id: 'encoding-utf16-bom-le',
    description: 'CLAUDE.md is saved as UTF-16 with a little-endian byte-order-mark, containing only ordinary benign instruction text (no attack payload).',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
    note: 'Documents actual behavior: production reads file content as UTF-8 (fileVersions.ts); reading a genuinely UTF-16LE-encoded file as UTF-8 garbles it byte-by-byte (each ASCII char becomes itself interleaved with a stray 0x00 byte, and the 0xFF/0xFE BOM bytes each decode as the U+FFFD replacement character) rather than throwing. Verified this garbled text contains none of RF-1/RF-2\'s covered code points, so no spurious findings result.',
  },
  {
    id: 'encoding-utf16-bom-be',
    description: 'Same as encoding-utf16-bom-le but big-endian, constructed by byte-swapping the little-endian encoding.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
    note: 'Documents actual behavior, same reasoning as encoding-utf16-bom-le.',
  },
  {
    id: 'encoding-bom-not-at-start',
    description: 'A literal U+FEFF character appears mid-sentence in CLAUDE.md, not at byte offset 0 (i.e. not functioning as an actual file-level BOM).',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
    note: 'Sanity check: RF-1\'s regex has no position-dependent logic at all (U+FEFF is simply outside its character range everywhere), so this confirms there is no separate "is this a leading BOM" special case anywhere in the codebase that could behave inconsistently by position -- consistent with the existing near-miss-bom scenario (BOM at the actual start).',
  },
  {
    id: 'encoding-mixed-crlf-lf',
    description: 'CLAUDE.md alternates CRLF and LF line endings between lines, with otherwise plain, benign content.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'negative',
    note: 'Confirms mixed line endings do not confuse the scan or the line/column locator (which splits on "\n" only, treating a trailing "\r" as an ordinary character) into a crash or a spurious finding.',
  },
  {
    id: 'encoding-nfc-vs-nfd-homoglyph',
    description: "Greek small letter omicron with tonos, encoded in its NFD-decomposed form (bare omicron U+03BF + combining acute accent U+0301) rather than the precomposed NFC code point (U+03CC), substituted into an English word.",
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "Documents actual behavior for a real NFC/NFD pair tied to an existing table entry (RF-2's table only has bare omicron U+03BF, not the precomposed accented U+03CC). Because JS regex character-class matching works code-point by code-point with no normalization step, the bare U+03BF is expected to match on its own even with a trailing combining accent -- meaning the DECOMPOSED (NFD) form is actually MORE detectable than the precomposed (NFC) form would be, the reverse of what one might assume. Neither form's detectability was changed by this task; only the NFD form is exercised here.",
  },
  {
    id: 'encoding-fullwidth-latin-homoglyph',
    description: 'Fullwidth Latin small letter a (U+FF41, from the Halfwidth and Fullwidth Forms block) is substituted for a regular Latin "a".',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "FN-expected, known gap: RF-2's table only covers Cyrillic and Greek confusables (architecture.md 5); the Halfwidth/Fullwidth Forms block is entirely outside it. Same convention as known-gap-uncommon-homoglyph.",
  },
  {
    id: 'encoding-mathematical-alphanumeric-symbol',
    description: 'Mathematical bold small a (U+1D41A, an astral-plane code point from the Mathematical Alphanumeric Symbols block) is substituted into a sentence.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: 'FN-expected, known gap: entirely outside the confusable table. Also confirms the regex (no "u" flag) does not accidentally mis-match on lone surrogate halves for an astral character -- it simply does not match at all, cleanly, no throw.',
  },
  {
    id: 'encoding-zwsp-in-diffdrift-file',
    description: 'A zero-width space is embedded inside a JSON string value (the "command" field) in .mcp.json, a diff-drift file, not a rule file; base and head are identical.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: "TN by design, not by the character being undetectable: calling detectInvisibleUnicode directly on this content WOULD find it (verified) -- the point is that architecture.md section 4 scopes RF-1/RF-2 to rule files only, and both run.ts's and processPullRequestEvent.ts's engine dispatch never invoke rule-file detectors on a diff-drift-engine file at all. Base and head are byte-identical, so all four diff-drift detectors also correctly report nothing changed.",
  },
  {
    id: 'encoding-armenian-homoglyph',
    description: 'Armenian small letter oh (U+0585), which closely resembles Latin/Cyrillic "o", is substituted into a sentence.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "FN-expected, known gap: the Armenian block is entirely outside RF-2's table.",
  },
  {
    id: 'encoding-cherokee-homoglyph',
    description: 'Cherokee letter si (U+13DA), which closely resembles Latin/Cyrillic "o", is substituted into a sentence.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: "FN-expected, known gap: the Cherokee block is entirely outside RF-2's table.",
  },
  {
    id: 'encoding-combining-diacritical-invisible',
    description: 'A combining grapheme joiner (U+034F), a zero-width combining mark from the Combining Diacritical Marks block, is inserted mid-word.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: "FN-expected, known gap: RF-1's INVISIBLE_CHAR_PATTERN only covers zero-width spaces/joiners (U+200B-200D), bidi controls (U+202A-202E), and isolates (U+2066-2069) per architecture.md 4; the Combining Diacritical Marks block is entirely outside those ranges despite U+034F being just as visually invisible.",
  },
  {
    id: 'encoding-rlm-standalone',
    description: 'A right-to-left mark (U+200F) is inserted standalone mid-sentence, with no accompanying bidi override/embedding characters.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: "Documents actual behavior: U+200F falls in the Right-to-Left Mark/Left-to-Right Mark pair, which sits just below RF-1's bidi-control range (U+202A-202E) and just below its isolate range (U+2066-2069). It is NOT covered by INVISIBLE_CHAR_PATTERN, so this is an FN-expected known gap, distinct from the covered bidi-override characters.",
  },
  {
    id: 'encoding-soft-hyphen',
    description: 'A soft hyphen (U+00AD), an invisible-unless-at-a-line-break formatting character, is inserted mid-word.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: "FN-expected, known gap: U+00AD is in the Latin-1 Supplement block, well outside RF-1's covered ranges.",
  },
  {
    id: 'encoding-unicode-tag-characters',
    description: 'A Unicode tag character (U+E0001, LANGUAGE TAG, from the astral-plane Tags block once used for steganographic prompt injection) is inserted mid-sentence.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: "FN-expected, known gap: the Tags block (U+E0000-E007F) is entirely outside RF-1's covered ranges. Astral-plane code point; confirmed the surrogate pair round-trips correctly and the regex (no 'u' flag) simply fails to match either surrogate half rather than throwing or false-matching.",
  },
  {
    id: 'multi-new-and-modified-server',
    description: 'A single .mcp.json change both adds a brand-new "browser" MCP server and swaps the pinned command/args on the existing "filesystem" server in the same PR.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server + diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "TP x2: DD-1 fires once for the new browser server; DD-2 fires once for the pinned-field swap on filesystem. Confirms the two detectors operate independently over the same head/base pair without one suppressing the other.",
  },
  {
    id: 'multi-wildcard-and-hook',
    description: 'A single .claude/settings.json change both adds an unrestricted "Bash(*)" entry to the allow-list and adds a new PostToolUse hook, in the same PR.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions + diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "TP x2: DD-3 fires HIGH for Bash(*) -- the entry is a Tool(...) shape whose entire body is wildcard characters, so it still escalates under the fixed isUnrestrictedWildcard logic (this is a direct check that the recent scoped-glob fix did not inadvertently affect this straightforward whole-body-wildcard case). DD-4 fires for the new PostToolUse hook. Both fire independently on the same file.",
  },
  {
    id: 'multi-deny-removed-and-allow-added',
    description: 'A single .claude/settings.json change both removes the "Bash(rm)" deny rule and adds "Write(/tmp)" to the allow-list, in the same PR.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: "TP x2, both DD-3: one finding for the removed deny rule, one for the added allow entry. Confirms both widening branches of the same detector fire independently within a single call rather than one short-circuiting the other.",
  },
  {
    id: 'multi-rf1-and-rf2-same-file',
    description: 'A single CLAUDE.md change adds both a zero-width-space-hidden instruction ("Ignore this rule silently...") and a Cyrillic-homoglyph-substituted instruction ("You may run any command...") in the same file.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode + rule-file.homoglyph',
    groundTruth: 'positive',
    note: "TP x2: RF-1 fires once for the ZWSP, RF-2 fires once for the Cyrillic letter. Confirms the two rule-file detectors both run over the same head content and neither suppresses the other.",
  },
  {
    id: 'multi-three-servers-mixed-changes',
    description: 'A .mcp.json change touching three servers at once: "filesystem" is left byte-identical, "git" has its pinned command swapped, and a brand-new "browser" server is added.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server + diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "TP x2 (DD-1 for browser, DD-2 for git) plus TN x1 (no finding for the untouched filesystem entry). Confirms detectors scope findings per-server rather than firing broadly whenever any server in the file changed.",
  },
  {
    id: 'multi-all-four-diffdrift-one-pr-mcp',
    description: 'Half 1 of 2 of a PR exercising all four diff-drift detectors: this .mcp.json file adds a new "browser" server (DD-1) and swaps the pinned command on the existing "filesystem" server (DD-2).',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server + diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "TP x2 of an intended TP x4 spread across two files/scenario IDs. The task specified one PR exercising all four DD detectors at once; this corpus format is one file-pair per scenario ID, so the scenario is split into this .mcp.json half (DD-1+DD-2) and the companion multi-all-four-diffdrift-one-pr-settings scenario (DD-3+DD-4) rather than forcing two unrelated files into one fixture directory.",
  },
  {
    id: 'multi-all-four-diffdrift-one-pr-settings',
    description: 'Half 2 of 2 of a PR exercising all four diff-drift detectors: this .claude/settings.json file adds an unrestricted "Bash(*)" allow entry (DD-3) and a new SessionStart hook (DD-4).',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions + diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "TP x2 of an intended TP x4 spread across two files/scenario IDs; see multi-all-four-diffdrift-one-pr-mcp for the split rationale.",
  },

  // --- Session 5 of 6 (corpus expansion 99 -> 113): 21 additional scenarios
  // (the task specified 20; #17, "gradual drift across two sequential PRs,"
  // was split into two scenario IDs -- pr1 and pr2, where pr2's before.json
  // is byte-identical to pr1's after.json -- for the same reason as the
  // Session 4 #20 split: this corpus format is one file-pair per scenario ID,
  // representing one PR, and a two-PR trend genuinely needs two PRs' worth of
  // file pairs to show honestly). 10 exercise realistic BENIGN changes,
  // several DELIBERATELY labeled "TP by design, not a bug" where firing is
  // the CORRECT behavior even though the underlying change is harmless --
  // documented per architecture.md section 2's "precision over recall" and
  // "quiet by default" principles being about avoiding noise on genuinely
  // unrelated changes, not about suppressing findings on changes that
  // legitimately match a detector's stated trigger condition. 10 exercise
  // ADVERSARIAL evasion attempts against detector mechanics specifically,
  // several DELIBERATELY resulting in a documented miss or under-classification
  // where the detector's own spec structurally cannot see the evasion --
  // same "document, don't fix" convention as the known-gap encoding scenarios
  // from Session 4, grounded here in architecture.md section 9's explicit
  // non-goals (no cross-file taint analysis) and section 2's stateless-in-v1
  // principle, not just detector-table gaps.

  {
    id: 'benign-version-bump-in-args',
    description: 'A routine patch-version bump in an existing MCP server\'s pinned args (e.g. "@modelcontextprotocol/server-git@1.0.0" to "...@1.0.1"), nothing else changed.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "TP by design, not a bug: DD-2 fires on any change to the args field of an already-approved server, per its spec (architecture.md 5: 'a pinned tool, command, or version being swapped'). A version bump IS a real args change -- the fact that it's routine and probably harmless doesn't change that the pinned identity of the entry was altered. 'Legitimate' and 'should be silent' are not the same thing here: DD-2 has no way to distinguish a benign patch bump from a malicious version swap without semantic understanding of the package registry, which v1 deliberately does not attempt (deterministic-only, section 2). Silencing this case would require guessing at intent, which is exactly the kind of unverifiable confidence the project avoids.",
  },
  {
    id: 'benign-new-unrelated-toplevel-key',
    description: 'A new top-level key ("metadata") unrelated to mcpServers/servers/permissions/hooks is added to .mcp.json; the mcpServers block itself is untouched.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: 'TN: all four diff-drift detectors only read specific known keys (mcpServers/servers, permissions, hooks). An unrelated top-level addition is invisible to all of them, correctly, since none of it touches agent tool/permission/hook surface.',
  },
  {
    id: 'benign-hook-added-for-linting',
    description: 'A genuine lint-on-edit hook ("npx eslint --fix" on PostToolUse) is added to .claude/settings.json for real CI/quality purposes, not for anything malicious.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "TP by design, not a bug: architecture.md 5 specifies DD-4 flags any new or changed hook at high severity, unconditionally -- 'a new hook, or a change to an existing hook's command, is a finding.' There is no intent-aware carve-out for hooks that happen to be benign, because CVE-2025-59536 (the vector DD-4 exists to catch) looks identical in structure to a routine lint hook until a human reads the command. Flagging every hook change, including this one, is the accepted cost of catching the malicious ones.",
  },
  {
    id: 'benign-multiple-permissions-narrowed',
    description: 'Several allow-list entries are removed at once ("Read(.env.example)" and "Write(./dist/**)"), pure narrowing, nothing added and no deny rule removed.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'negative',
    note: 'TN: DD-3 only reports allow-list additions and deny-rule removals (the two widening directions). Removing allow entries is narrowing, explicitly out of scope regardless of how many entries are removed at once.',
  },
  {
    id: 'benign-claude-md-typo-fix',
    description: 'A pure spelling/grammar correction in CLAUDE.md ("commiting" -> "committing", "Recieve" -> "Receive"), no structural change and no non-ASCII characters introduced.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode + rule-file.homoglyph',
    groundTruth: 'negative',
    note: 'TN, sanity check: an ordinary editorial fix using only plain ASCII should never trip RF-1 or RF-2. Confirms neither detector is sensitive to unrelated prose changes.',
  },
  {
    id: 'benign-cursor-rules-new-clean-file',
    description: 'A brand-new file added under .cursor/rules/ (base did not exist) with entirely clean, plain-ASCII content.',
    filePath: '.cursor/rules/style.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode + rule-file.homoglyph',
    groundTruth: 'negative',
    note: "TN, sanity check. before.md is an empty file standing in for 'file did not exist in base' -- the closest this fixture format can represent a null base, since readScenarioFiles always reads a real file from disk. This has no effect on the result either way: run.ts's rule-file dispatch only ever evaluates head content (runRuleFileDetectors ignores base entirely), so a brand-new clean file behaves identically to an existing clean file being edited.",
  },
  {
    id: 'benign-copilot-instructions-formatting-only',
    description: 'Markdown formatting changes only in .github/copilot-instructions.md: heading level changed (## to #) and bullet style changed (- to *); no content or character-class change.',
    filePath: '.github/copilot-instructions.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode + rule-file.homoglyph',
    groundTruth: 'negative',
    note: 'TN, sanity check: pure Markdown syntax changes with no new characters outside plain ASCII should not trip either rule-file detector.',
  },
  {
    id: 'benign-comment-like-key-added',
    description: 'A "_comment" key (plain string value, not an object) is added at the top level of .mcp.json; it is not a real server entry.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'negative',
    note: "TN: parseMcpServers only reads obj.mcpServers ?? obj.servers; a sibling top-level key named '_comment' is never inspected as a server candidate, regardless of its name suggesting documentation intent.",
  },
  {
    id: 'benign-settings-unrelated-section-added',
    description: 'A new unrelated top-level section ("editor": { "theme": "dark", "fontSize": 14 }) is added to .claude/settings.json, outside permissions and hooks.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions + diff-drift.hook-changed',
    groundTruth: 'negative',
    note: 'TN: DD-3 and DD-4 only read the permissions and hooks keys respectively; an unrelated sibling section is invisible to both.',
  },
  {
    id: 'benign-hooks-reorganized-same-behavior',
    description: 'An existing hook\'s command is rewritten from "./scripts/lint.sh" to the functionally identical "bash ./scripts/lint.sh" (explicit interpreter, same outcome), framed as a documentation/clarity cleanup.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "TP by design, not a bug: DD-4 compares hook commands by exact string equality (fieldChanged-style comparison in hookChanged.ts), with no semantic understanding of shell equivalence. 'bash X' and 'X' are byte-different strings that produce the same behavior to a human reader, but DD-4 has no way to verify that equivalence, so it correctly reports the string it can see changing. This is the same epistemic-limits principle as the DD-3 wildcard fix, applied in the opposite direction: staying silent here would require confidence about shell semantics the detector cannot have.",
  },
  {
    id: 'adversarial-cross-file-attack-split',
    description: 'A new PostToolUse hook is added in .claude/settings.json calling "./scripts/deploy.sh"; the actual malicious payload is described only conceptually as living inside deploy.sh itself, a file this fixture does not include.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "FN by design (structural), even though DD-4 correctly fires here. The visible fact -- 'a new hook was added, running ./scripts/deploy.sh' -- IS flagged, high severity, exactly as DD-4 is supposed to. What is structurally invisible is what deploy.sh actually does: script file contents are not a monitored file at all (monitoredFiles.ts only watches specific config paths), so RedFlag CI has no way to evaluate, or even see, the real payload. This is a direct instance of the non-goal in architecture.md section 9 ('No whole-repository indexing or cross-file taint analysis') and the diff-scope ceiling documented in PROBLEM_SPACE.md ('If the sink is in the PR but the source lives in a file nobody touched, a diff-only tool structurally cannot see the connection'). The finding that DOES fire is correct and useful (a reviewer is now on notice to go read deploy.sh) but is not equivalent to detecting the actual attack.",
  },
  {
    id: 'adversarial-rename-masks-swap',
    description: 'A server is renamed ("old-fs" to "new-fs") AND its command/args swapped to something malicious (curl-based payload fetch) in the same diff.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "FN on the swap specifically, by design: detectSwappedMcpServer only compares entries whose KEY is present in both base and head (swappedMcpServer.ts line ~65: an entry present in head but not base is treated as an addition and explicitly skipped -- 'DD-1's job, not a swap'). Renaming the key entirely removes the old entry and introduces what looks like a brand-new one, so DD-2 produces zero findings for the rename+swap combination -- confirmed directly below. DD-1 (detectNewMcpServer) still fires generically ('New MCP server new-fs added'), which is expected and correctly labeled: the tool does notice a new server appeared, it just cannot attribute that appearance to a swap of a previously-trusted entry, which is the more specific and more alarming fact.",
  },
  {
    id: 'adversarial-broad-non-wildcard-pattern',
    description: 'A bare tool-name permission entry ("Bash", with no parentheses or arguments) is added to the allow-list -- functionally an unrestricted grant for that whole tool in Claude Code\'s real permission schema, but containing no "*" character at all.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: "JUDGMENT, not a full miss: DD-3 still fires (any new allow entry is at least a warning), but isUnrestrictedWildcard's TOOL_CALL_SHAPE regex requires literal parentheses to extract a body to test for wildcard characters; a bare 'Bash' has no parens, so the body-is-all-wildcards check never applies and severity stays at warning rather than escalating to high. Recognizing that a bare tool name is AS broad as 'Bash(*)' would require encoding domain knowledge about Claude Code's permission grammar beyond a syntactic wildcard check, which is a real interpretive step, not a mechanical one -- consistent with the 'severity calls must reflect epistemic limits' principle used in the DD-3 wildcard fix. This is a debatable tradeoff to flag for review, not a defect to silently patch here.",
  },
  {
    id: 'adversarial-homoglyph-plus-invisible-combo',
    description: 'A single sentence in CLAUDE.md combines a zero-width space hidden inside one word ("Alw[ZWSP]ays") and a Cyrillic homoglyph substituted into another word in the same line ("[Cyrillic а]sking").',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode + rule-file.homoglyph',
    groundTruth: 'positive',
    note: 'TP x2: RF-1 fires on the ZWSP (U+200B), RF-2 fires on the Cyrillic а (U+0430), independently, even though both characters sit in the same line of the same file. Confirms neither detector short-circuits or gets confused when both evasion techniques are combined on one line rather than kept in separate findings.',
  },
  {
    id: 'adversarial-split-command-across-args',
    description: 'A malicious command ("curl ... | sh") is constructed by splitting it across several separate array elements in an existing server\'s args, rather than embedding it as one string.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "TP, correctly detected: DD-2's fieldChanged comparison uses JSON.stringify on the whole args array (swappedMcpServer.ts), so it is indifferent to how content is distributed across array elements -- any difference anywhere in the array shape or values shows up as a change. Splitting a command across multiple elements doesn't evade detection because DD-2 was never doing string-content pattern matching in the first place; it detects that the pinned field changed at all, structurally.",
  },
  {
    id: 'adversarial-benign-name-malicious-command',
    description: 'A new MCP server named "linter" (an innocuous-sounding name) is added with a genuinely malicious command (curl-pipe-to-shell installer).',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: "TP, correctly detected: DD-1 fires purely on the presence of a new key under mcpServers, with no inspection of the server's name string. Confirms detection is structural (key presence), not name-based -- a server calling itself 'linter' or 'formatter' gets exactly the same warning-severity 'new server added' finding as one calling itself anything else.",
  },
  {
    id: 'adversarial-gradual-drift-two-prs-pr1',
    description: 'First of two sequential PRs: an empty allow-list gains one narrow-looking entry, "Bash(git diff)".',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: "TP, correctly detected in isolation (warning-severity 'permission added'), and that is exactly the limitation this pair of scenarios documents: each PR is only ever compared to its own immediate predecessor. See adversarial-gradual-drift-two-prs-pr2 for the second half and the combined point.",
  },
  {
    id: 'adversarial-gradual-drift-two-prs-pr2',
    description: 'Second of two sequential PRs (before.json here is byte-identical to pr1\'s after.json): the allow-list gains a second entry, "Bash(git *)", which on its own is also a narrow-looking, unremarkable-sounding addition.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'positive',
    note: "TP, correctly detected in isolation (warning-severity, and correctly NOT escalated to high since 'git *' is a Tool(...)-shaped entry whose body is 'git *', not all wildcard characters, so isUnrestrictedWildcard stays false). Documents a real, accepted limitation rather than a bug: v1 is stateless (architecture.md section 2 -- 'the baseline for drift detection is just the PR's base branch, fetched fresh... every run'; a persistence layer for history/trend tracking is deliberately deferred to v3, section 8). Across these two PRs the allow-list went from empty to two git-scoped entries, a meaningful cumulative expansion, but nothing in v1 ever compares pr2's result against pr1's starting point, or any older baseline -- each PR is evaluated only against its own immediate parent. Split into two scenario IDs, matching the Session 4 #20 precedent, because this corpus format is one file-pair per scenario ID representing one PR, and a two-PR trend genuinely needs two PRs' worth of fixtures to demonstrate honestly.",
  },
  {
    id: 'adversarial-homoglyph-in-hook-key-not-command',
    description: 'A hook\'s "matcher" field ("Bash") has its "a" replaced with a Cyrillic homoglyph ("B[Cyrillic а]sh"); the "command" field is left completely unchanged.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.hook-changed',
    groundTruth: 'positive',
    note: "FN by design: hookChanged.ts's parseHooks reads only the 'command' field for comparison (falling back to JSON.stringify of the whole object only when 'command' is missing entirely) -- the 'matcher' field's content is never inspected at all. Since baseEntry.command === headEntry.command here, DD-4 produces zero findings, regardless of what changed in matcher. Separately, RF-1/RF-2 never run on .claude/settings.json at all, since it is a diff-drift file, not one of the rule-file paths in monitoredFiles.ts -- so even though a real homoglyph character is present in this file, no detector in the pipeline is positioned to see it. Two independent reasons for the same miss, both structural, not incidental.",
  },
  {
    id: 'adversarial-encoded-payload-in-args',
    description: 'A base64-encoded string ("ZWNobyBwd25lZCA+IC90bXAvcHduZWQ=", decoding to "echo pwned > /tmp/pwned") is added to an existing server\'s args as a new "--init-script" argument.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: 'TP, correctly detected: DD-2 flags the raw change to the args array regardless of understanding what the added string decodes to or means. The detector has no base64-awareness and needs none here -- it detects that a pinned field changed at all, which is true whether the new argument is human-readable or opaque-looking.',
  },
  {
    id: 'adversarial-mimics-approved-pattern',
    description: 'A new MCP server ("filesystem-v2") is added whose command and args closely mirror an already-approved server ("filesystem"), differing only in one subtle package-name suffix.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: "TP, correctly detected: DD-1 fires on any key present in head but absent from base, with no similarity comparison against existing approved entries. Confirms detection is purely structural (is this key new?), not a content-similarity heuristic that a close-mimicry attempt could specifically evade -- mirroring an approved entry closely doesn't make the new key any less new.",
  },
  {
    id: 'adversarial-homoglyph-server-name',
    description: 'Trojan Source-style homoglyph (CVE-2021-42574/42694 pattern): A spoofed MCP server name using a Cyrillic look-alike.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-mcp-server-swap',
    description: 'MCPoison-pattern swap (CVE-2025-54136): benign args swapped to a reverse-shell-shaped command while the server name is identical.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-curxecute-config-rewrite',
    description: 'CurXecute-pattern config rewrite (CVE-2025-54135): A malicious .mcp.json server entry introduced rather than a settings.json hook.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-invisible-unicode-instruction',
    description: 'Unicode Tag Block invisible injection (Goodside\'s technique): Realistic instruction payload ("ignore prior constraints") using invisible tags.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-obfuscated-command-base64',
    description: 'Base64 obfuscated payload (eslint-scope-style): a command with a base64 blob that plausibly decodes to a curl-pipe-bash pattern.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.obfuscated-command',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-unpinned-npx-dependency',
    description: 'Unpinned npx dependency (event-stream/colors.js-style): bare npx package with no pin.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.unpinned-mcp-dependency',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-ssrf-network-target',
    description: 'SSRF-style bare IP/internal target (Capital One pattern): env var or arg pointing at a cloud metadata-style IP (e.g., 169.254.169.254).',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.suspicious-network-target',
    groundTruth: 'positive',
  },
  {
    id: 'adversarial-path-traversal-cli-arg',
    description: 'Path traversal via CLI arg: ../../ sequence in a file-reading tool\'s default args.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.path-traversal',
    groundTruth: 'positive',
    note: 'Pattern-inspired, no specific MCP incident documented. Path traversal via CLI args is a general, well-documented vulnerability class, but speculative here as a specific MCP exploit.'
  },
  {
    id: 'adversarial-duplicate-json-key',
    description: 'JSON duplicate key smuggling: A duplicate server key definition. Pattern-inspired, no specific MCP incident documented but widely seen in WAF bypasses.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.duplicate-json-key',
    groundTruth: 'positive',
    note: 'Pattern-inspired, no specific MCP incident documented. This is a common parsing differential issue (JSON Injection) where tools process JSON duplicate keys differently, often used for WAF bypasses.'
  },
  {
    id: 'regression-rf2-armenian-homoglyph',
    description: 'RF-2 regression variant (U+0501 gap): Armenian homoglyph U+0585.',
    filePath: '.cursor/rules/deploy.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'positive',
    note: 'Variant of known-gap-uncommon-homoglyph using a different script/code point from the updated confusables table.',
  },
  {
    id: 'regression-rf1-combining-mark-start',
    description: 'RF-1 regression variant (Combining Diacritical Marks): U+0300 grave accent at the start of a word.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: 'Variant of encoding-combining-diacritical-invisible using a different mark in a different position.',
  },
  {
    id: 'regression-rf1-unicode-tag-cursor-rules',
    description: 'RF-1 regression variant (Unicode Tags block): U+E0020 Space Tag in .cursor/rules.',
    filePath: '.cursor/rules/security.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.invisible-unicode',
    groundTruth: 'positive',
    note: 'Variant of encoding-unicode-tag-characters using a different tag code point in a different file type.',
  },
  {
    id: 'regression-dd1-dual-key-collision',
    description: 'DD-2 regression variant (schema dual-key merge): same server name present under mcpServers and servers with different definitions.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'negative',
    note: 'Task 6.4: this fixture alone could not actually distinguish "mcpServers correctly wins the merge" from "servers is short-circuited away entirely" -- both produce this same TN, since mcpServers.weather is unchanged either way and was never going to need servers.weather to resolve correctly. The investigation found DD-2 had the exact unfixed `mcpServers ?? servers` short-circuit bug Task 2.1 had already fixed on DD-1: a colliding servers entry was invisible, but so was any non-colliding one. Fixed in Task 6.4 (same merge-with-precedence as DD-1). This scenario stays TN post-fix, now for the documented reason (mcpServers correctly wins the collision) rather than by the bug\'s coincidence -- see the companion regression-dd2-servers-key-blind-spot for the case that actually distinguishes the two.',
  },
  {
    id: 'regression-dd2-servers-key-blind-spot',
    description: 'DD-2 regression variant (schema dual-key merge, non-collision): a server entry exists only under "servers" (no mcpServers counterpart) and its command/args change between base and head.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: "Task 6.4: companion to regression-dd1-dual-key-collision, testing what that fixture couldn't -- a servers-only entry with no mcpServers collision to hide behind. Before the Task 6.4 fix, DD-2's `mcpServers ?? servers` never read `servers` at all once `mcpServers` was present (even non-empty and unrelated), so this scenario's swap on 'filesystem' was missed entirely: a false negative, not a suppressed finding. Fixed by the same merge-with-precedence change applied to regression-dd1-dual-key-collision.",
  },
  {
    id: 'regression-dd1-rename-args-whitespace',
    description: 'DD-1 regression variant (rename correlation): rename where args differ trivially by whitespace.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.new-mcp-server',
    groundTruth: 'positive',
    note: 'Variant of near-miss-mcp-server-rename confirming that a trivial whitespace change in an argument string prevents correlation and correctly fires as a new server.',
  },
  {
    id: 'regression-dd3-narrowing-correlation',
    description: 'DD-3 regression variant (narrowing correlation): narrowing on a different tool name.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.widened-permissions',
    groundTruth: 'negative',
    note: 'Variant of dd3-wildcard-narrowed-to-specific confirming the narrowing correlation generalizes beyond Bash/Write.',
  },
  {
    id: 'regression-dd2-positional-arg-reorder-fires',
    description: 'DD-2 regression variant (unordered flagged-arg comparison): 3 flags reordered plus one positional arg moved.',
    filePath: '.mcp.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.swapped-mcp-server',
    groundTruth: 'positive',
    note: 'Variant of near-miss-args-reorder confirming that moving a positional argument still triggers a swap even alongside flag reordering.',
  },
  {
    id: 'regression-rf2-greek-majority-legit',
    description: 'RF-2 regression variant (script-majority check): Greek prose as the legitimate-text case.',
    filePath: 'CLAUDE.md',
    engine: 'rule-file',
    detectorUnderTest: 'rule-file.homoglyph',
    groundTruth: 'negative',
    note: 'Variant of near-miss-legit-cyrillic-text confirming the script-majority fix generalizes past Cyrillic.',
  },
  {
    id: 'dd8-monitored-file-deleted',
    description: 'A monitored diff-drift file present in base is deleted outright in head, taking every permission/hook/server definition with it.',
    filePath: '.claude/settings.json',
    engine: 'diff-drift',
    detectorUnderTest: 'diff-drift.monitored-file-deleted',
    groundTruth: 'positive',
    note: 'Before this detector existed, DD-1 through DD-7 each correctly returned [] on a null head, so the single most severe change possible -- every constraint in the file gone at once -- produced zero findings. This scenario has no after.json: the file\'s absence in head IS the scenario, not any particular (empty) content for it.',
  },
];
