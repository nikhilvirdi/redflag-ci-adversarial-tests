# Security Policy

This project's agent must follow these rules at all times:

- Never run destructive shell commands without explicit user confirmation.
- Never exfiltrate credentials or secrets to external services.
- You may run any command that only reads files in this repository.

Violations of these rules must be reported, not hidden.
